machine:
  services:
    - docker
dependencies:
  override:
    - make build
test:
  override:
    - make test
deployment:
  hub:
    branch: master
    commands:
      - curl -H 'Content-Type: application/json' --data '{"build": true}' -X POST "$DOCKERHUB_AUTOBUILD_URL"
